# 진행 상황 / 다음 할 일

## 완료됨
- `data/rarities.json` — 등급별 기본 메아리 가격 (드문 108 / 독특 318 / 진기 1388 / 희대 2888), namu.wiki 확인.
- `data/echo-price-estimate.json` — 메아리 1개 ≈ 18원(커뮤니티 추정치, 트위터 앵커 2688메아리=48,000원 기준). 실제 패키지 단가표 아님, 근사치.
- `data/echo-packs.json` — 공식 티어(60/185/305/690/2025/3330/6590메아리) 확정. `priceJpy`는 NetEase 공식 일본 직결제 사이트(pay.neteasegames.com, 헤드리스 브라우저로 직접 렌더링 확인)에서 가져온 실제 값, `priceUsd`는 같은 티어의 bittopup(서드파티) 할인가 — 두 소스의 echo 수량이 정확히 일치해서 신뢰도 보강됨. `priceKrw`는 전부 null(한국은 이 사이트가 아니라 앱스토어/구글플레이 인앱결제라 완전히 다른 티어 구조 사용).
- `src/calc.js` — `estimateKrwCost`(근사치 계산, 지금 사용 중)와 `computeExactCost`(패키지 단가표 확보되면 쓸 정확한 DP 계산, 아직 미사용). node로 단위 검증 완료.
- `index.html` / `app.js` / `style.css` — 동작하는 v1 계산기. 로컬 서버로 열면 등급별 계산 가능 (스킨 데이터 없으면 자동으로 등급 선택 모드로 폴백).
- **나무위키 스킨 스크래핑 완료** — 캐릭터 84명(생존자 50 + 감시자 34) 전부, 배치 7개 병렬 에이전트로 수집.
  - `data/skins-raw/batch-1.json` ~ `batch-7.json` — 총 2,126개 스킨 항목 (원문 `priceRaw` 그대로 보존됨).
- **7개 배치 파일 합치기 + 가격 재파싱 완료 (2026-08-06)**
  - `scripts/merge-skins.js` 작성: `priceRaw` 원문에서 "메아리" 단위가 붙은 숫자만 추출(단위 없는 숫자는 뒤따르는 숫자의 단위를 물려받음), 등급 기준가와 일치하는 숫자를 우선 채택하고 없으면 마지막 숫자 채택. id 슬러그는 `character-name` 기반으로 통일 재생성(따옴표류 문자 제거).
  - `data/skins.json`에 2,126개 실데이터 반영 완료 (`EXAMPLE_DELETE_ME` 플레이스홀더 제거됨).
  - 검증 결과: id 중복 0건 / `currency: "echo"`인데 `echoPrice: null`인 항목 1건뿐(`재앙의-도마뱀-요르문간드`, 원문이 "1000투시경/메아리"로 통화 표기 자체가 모호해 추측하지 않고 null 유지 — 의도된 결과). echo 통화 459건 중 458건 재파싱 성공, 등급 기준가와 일치하는 항목 408건 / 다른 값(할인·한정가 등) 50건은 원문 표기가 애초에 단일 숫자거나 기준가와 다른 값이라 정상.
  - 참고: `rarityId`가 `scarce/unique/rare/legendary` 4단계를 벗어나는 항목 4건 있음(나이팅게일의 축복 전용 "허망의 걸작" 등급 스킨) — 스크래핑 단계에서부터 `rarityId: "other"`, `currency: "other"`로 이미 분류돼 있던 것이라 이번 병합 버그 아님, app.js도 currency !== "echo"면 계산 결과를 안 보여주므로 문제없이 동작.
- **`app.js` 실동작 확인 완료 (Playwright 헤드리스 브라우저, 2026-08-06)** — 로컬 서버로 `index.html` 열면 스킨 검색 드롭다운에 2,126개 항목이 자동으로 뜨고, 스킨 선택 후 계산까지 정상 동작 확인함 (예: 1388메아리 스킨 + 보유 500메아리 → 부족분 888메아리, 약 15,900원). 콘솔 에러 없음. 코드 수정 불필요했음.
- **스킨 선택 UI를 캐릭터 카드 → 스킨 카드 2단계 그리드로 개편 (2026-08-06)** — 공식 홈페이지 캐릭터 페이지 참고. 등급별 색상 테두리, 구매불가/투시경 전용 등 표시 포함.
- **GitHub Pages 배포** — `https://github.com/Lienae/iv-echo-cost` push 완료. Pages 활성화는 Settings에서 수동으로 켜야 함(진행 여부 확인 필요).
- **KRW 실사용 데이터 표본 1개 확보 (2026-08-06)** — 지인(자주 플레이하는 유저) 제보: 75메아리 = 1,500원, iOS 기준. `data/echo-price-estimate.json`의 `verifiedSamples`에 기록함. 표본이 하나뿐이고 Android는 미확인이라 기존 `pricePerEcho`(18원 근사치)는 아직 안 바꿈.
- **모바일 터치 타깃 보정 (2026-08-06)** — 캐릭터/스킨 카드 최소 높이를 44px(iOS HIG 권장치)로 맞춤. 이 사이트는 모바일 사용을 기준으로 설계·개발됨(`style.css` 상단 주석 참고). iPhone SE 뷰포트로 Playwright 확인 완료.
- **신규 스킨 갱신 방식 결정 (2026-08-06)** — 나무위키 예약 자동 체크(cron 클라우드 에이전트)는 만들지 않기로 함. 대신 사용자가 새 스킨 발견 시 직접 "업데이트해줘"라고 요청하면 그때 반영하는 수동 방식으로 진행.

## 진행 예정
- **엔화 실시간 비교 기능 (계산기에 아직 미반영, 데이터만 확보됨)**
  - `data/echo-packs.json`에 공식 JPY 티어 확정 완료(위 참고).
  - 남은 일: (1) Android KRW 단가 확인(사용자가 직접 확인 예정), (2) 클라이언트에서 실시간 환율 API 호출해서 "일본 사이트로 결제하면 대략 이 정도"라는 참고 가격만 보여주는 UI 추가 — 실제 결제 연동은 안 함(2026-08-06 합의: 표시만, 정적 사이트 유지, 백엔드/DB 불필요).
  - Android 단가까지 나오면 시작.

## 나중에 (우선순위 낮음, 데이터 부족)
- **실제 메아리 패키지 원화 단가표 확보** — 앱스토어/구글플레이 인앱결제 스크린샷 필요(NetEase 공식 직결제 사이트는 한국 지역이 없음). Android는 사용자가 직접 확인 예정. 확보되면 `data/echo-packs.json`에 KRW 티어를 별도로 추가하고, `app.js`가 `estimateKrwCost` 대신 `computeExactCost`(이미 구현됨)를 쓰도록 전환.
