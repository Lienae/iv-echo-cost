# 진행 상황 / 다음 할 일

## 완료됨
- `data/rarities.json` — 등급별 기본 메아리 가격 (드문 108 / 독특 318 / 진기 1388 / 희대 2888), namu.wiki 확인.
- **나무위키 스킨 스크래핑 + 병합 완료** — 캐릭터 84명(생존자 50 + 감시자 34), 스킨 2,126개. `data/skins-raw/batch-1~7.json`이 원본, `scripts/merge-skins.js`가 `priceRaw`를 재파싱해서 `data/skins.json`을 생성함 (등급 기준가와 일치하는 숫자 우선, 없으면 마지막 숫자 채택 규칙). `skins-raw/`에 파일을 더 추가하고 스크립트를 다시 돌리기만 하면 자동 반영됨(하드코딩된 파일 목록 없음).
- **결제 기준 선택 + 정확 계산 (2026-08-06)** — `data/echo-packs.json`을 `bases`(결제 채널별 단가표) 구조로 재작성:
  - `kr_ingame`: 한국 인게임 결제, 사용자가 준 게임 내 충전화면 스크린샷(iOS/Android 각 1장, 가격표 동일) 기준 8개 티어(75~8248메아리, 1,500~149,000원). **공식 확정값.**
  - `jp_official`: 일본 공식 사이트(`pay.neteasegames.com`) 직결제, 헤드리스 브라우저로 직접 렌더링 확인한 7개 티어(60~6590메아리, 120~12,000円). 해외결제 가능한 카드를 그 사이트에 등록해서 엔화로 결제하는 방식(계정 지역이 아니라 카드 기준).
  - `src/calc.js`의 `computeExactCost`(동전 교환 DP)를 이 두 단가표에 맞춰 필드명 일반화(`priceKrw`→`price`, 통화 무관)하고 계산기 기본 로직으로 승격. 근사치(`estimateKrwCost`, 18원/메아리)는 더 이상 안 씀 — `data/echo-price-estimate.json`은 기록용으로만 남김.
  - `index.html`/`app.js`: 메아리 입력란 위에 "결제 기준" 라디오 버튼 추가. 기준 선택 → 그 기준의 단가표 리스트 표시 → 스킨 선택하면 그 기준으로 최소 비용 조합(예: "820메아리×1 + 209메아리×2 + 75메아리×2")까지 계산. 일본 기준 선택 시 실시간 환율(open.er-api.com, 클라이언트에서 직접 호출) 기반 원화 참고값도 같이 표시(표시만, 실제 결제 연동 없음).
- **UI/UX**: 스킨 선택은 캐릭터 카드 → 스킨 카드 2단계 그리드(등급별 색상, 구매불가 표시). 모바일 우선 설계(터치 타깃 44px, `style.css` 상단 주석 참고), Playwright로 데스크톱/모바일/라이트/다크 전부 확인함.
- **배포**: `https://github.com/Lienae/iv-echo-cost`에 push 완료, SSH 키 등록해둠(토큰 재발급 불필요). GitHub Pages 활성화는 Settings에서 수동으로 켜야 함(진행 여부 확인 필요).
- **신규 스킨 갱신 방식**: 예약 자동화 안 함. 사용자가 새 스킨 발견 시 직접 요청하면 그때 반영.

## 나중에 (우선순위 낮음)
- Android/iOS KRW 단가표(`kr_ingame`)가 바뀌거나 새 티어가 생기면 스크린샷 다시 받아서 `data/echo-packs.json` 갱신.
- `jp_official` 카드 결제 시 실제 카드사 환전 수수료율까지는 반영 안 함(현재는 실시간 환율만 참고로 보여줌) — 필요해지면 논의.
